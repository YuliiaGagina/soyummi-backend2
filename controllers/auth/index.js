const register = require("./register");
const login = require("./login");
const logout = require("./logout");
const getUsers = require("./getUsers");
const refresh = require("./refresh");
const activates = require("./activates");

module.exports = {
  register,
  login,
  logout,
  getUsers,
  refresh,
  activates,
};
